export enum ESource {
    ONLINE = 1,
    OFFLINE = 2,
    SALE = 3,
}

export enum ENotificationAction {
    PUSH_NOTIFICATION = 'PUSH_NOTIFICATION',
    SEND_SMS = 'SEND_SMS',
    SEND_EMAIL = 'SEND_EMAIL',
}